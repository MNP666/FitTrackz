[package]
name = "fit-core"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "1"   # ergonomic custom error types
serde     = { version = "1", features = ["derive"] }  # FitMetadata serialisation (needed for PyO3 later too)
