[package]
name = "fit-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
fit-core     = { path = "../fit-core" }
serde        = { version = "1", features = ["derive"] }
serde_json   = "1"
toml         = "0.8"
